# 企业级订单系统架构

客户端经 Gateway 进入订单、商品、库存、支付服务。订单创建时同步完成商品重新报价与 MySQL 库存预占；订单、支付各自在本地事务写业务表和 Outbox，独立发布器投递 RabbitMQ。订单服务消费支付成功，库存服务消费订单已支付/已取消，查询服务消费订单事件形成 CQRS 投影。

一致性边界是“单服务本地事务”，跨服务使用至少一次事件与幂等消费，不引入跨库大事务。MySQL 是订单和普通库存的最终事实源。
