# 最终一致性与补偿

业务表与 `outbox_event` 同事务提交；发布器扫描 NEW/RETRY 事件，成功后标记 SENT，失败则指数退避。消费者先插入 `(consumer_group,event_id)` 唯一记录，再执行业务并在同一事务标记成功。库存确认/释放还带 `RESERVED` 状态条件，形成双层幂等。

库存预占成功但订单落库失败时立即释放；释放接口失败由 reservation 过期扫描兜底。支付成功与超时关闭都只能从 `WAIT_PAY` 条件更新，只有一个结果生效；“关闭后支付成功”写入对账异常表。
