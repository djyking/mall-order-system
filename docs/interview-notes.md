# 设计讲解要点

1. 防超卖依赖 `available_stock >= quantity` 的单条条件更新，而不是全局分布式锁。
2. Outbox 解决数据库提交与消息发送的双写问题；至少一次投递通过数据库唯一键和状态条件实现业务幂等。
3. 支付成功与超时关闭的本质是同一旧状态上的 CAS 竞争；异常晚到支付进入对账流程。
4. 分片键服务高频查询，路由表解决订单号定位，CQRS 解决运营侧跨分片检索。
5. Redis 用于缓存、令牌、限流和秒杀快速判断，普通库存事实仍在 MySQL。
