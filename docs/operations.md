# 运行与故障演练

先启动基础设施，再启动 8081–8086 服务，最后启动 8080 Gateway。故障开关统一位于订单服务 `debug.failure` 配置前缀；演练应覆盖库存预占后订单失败、Outbox 首次发送失败、重复支付通知、重复 MQ 和超时预占释放。

生产部署必须替换示例账号、JWT 密钥，启用 Nacos 鉴权与 RabbitMQ TLS，并将告警连接到企业通知渠道。
